'use strict';

// We import all the classes needed by the View (VIP Architecture).
const roverMovementInteractor = require('../interactor/roverMovementInteractor');
const terrainInteractor = require('../interactor/terrainInteractor');

// We export all the classes needed by the View (VIP Architecture).
module.exports.terrainInteractor = roverMovementInteractor;
module.exports.terrainInteractor = terrainInteractor;

